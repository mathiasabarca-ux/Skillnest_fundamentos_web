// Función simple
function saludar() {
   console.log("¡Hola, bienvenido!");
}
alert
// Llamada a la función
saludar();
saludar();
saludar();
// Función con parámetro
function saludar(nombre) {
   console.log("¡Hola, " + nombre + "!");
}

// Llamadas con argumentos
saludar("Luis");
saludar("Ana");
// Función con return
function encontrarMayor(a, b) {
   if (a > b) {
       return a;
   } else {
       return b;
   
{

// Uso del valor devuelto
let numero1 = 10;
let numero2 = 7;
let maximo = encontrarMaximo(numero1, numero2);
//maximo guardala el valor de retorno
alert("El número mayor entre", ${numero1}, "y", numero2, "es:", maximo);

alert("El número mayor entre " + numero1 + " y " + numero2 + " es: " + maximo);
   }
function operaciones(a, b, c) {
   let suma = a + b + c;
   let promedio = suma / 3;
   return promedio;
}
fuction maximo(a, b) {
  
 } if (a > b) {
       return a;

   } else {
       return b;
   }